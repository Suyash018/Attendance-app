import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const a ="https://sheets.googleapis.com/v4/spreadsheets//values/{Sheet-Name}?valueRenderOption=FORMATTED_VALUE&key={API-Key}";

const Check = () => {
  return (
    <View>
      <Text>Check</Text>
    </View>
  )
}

export default Check

const styles = StyleSheet.create({})